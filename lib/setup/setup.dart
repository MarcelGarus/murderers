import 'package:flutter/material.dart';

import 'select_mode.dart';

class SetupJourney extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SelectModeScreen();
  }
}
