// This is a basic Flutter widget test.
// To perform an interaction with a widget in your test, use the WidgetTester utility that Flutter
// provides. For example, you can send tap and scroll gestures. You can also use WidgetTester to
// find child widgets in the widget tree, read text, and verify that the values of widget properties
// are correct.

/*import 'dart:async';
import 'dart:io';

import 'package:flutter_driver/driver_extension.dart';
import 'package:flutter_driver/flutter_driver.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:murderers/main.dart' as app;*/

void main() {
  /*enableFlutterDriverExtension();
  app.main();

  group('sample screenshots', () {
    //final introNextFinder = find.byValueKey('');
    FlutterDriver driver;

    setUpAll(() async => driver = await FlutterDriver.connect());
    tearDownAll(() async => await driver?.close());

    test('take sample screenshots', () async {
      print('Waiting for flutter driver');
      await driver.waitUntilNoTransientCallbacks();
      // TBD: when #11021 has been resolved, this shouldn't be necessary.
      await new Future<void>.delayed(const Duration(milliseconds: 500));
      
      final List<int> pixels = await driver.screenshot();
      final File file = new File('/home/marcel/Desktop/output.png');
      await file.writeAsBytes(pixels);
      print('wrote $file');
    });
  });*/
}
